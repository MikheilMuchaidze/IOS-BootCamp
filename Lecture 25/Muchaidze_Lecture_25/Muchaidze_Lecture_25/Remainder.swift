import Foundation
import UIKit

class Remainder {
    
    let note: String
    let category: String
    
    init(note: String, category: String) {
        self.note = note
        self.category = category
    }
}
